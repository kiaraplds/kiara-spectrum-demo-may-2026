# local-server

You are in a React Spectrum project that uses Yarn workspaces.

1) If node_modules is missing, run `yarn install` to install dependencies.
2) Start Storybook with `yarn start`.
3) Wait for the build to complete and print the local URL (typically http://localhost:9003).
4) Confirm the server is responding before reporting back.

Run the needed shell commands.